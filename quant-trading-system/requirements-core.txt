# 核心依赖（数据库 + FastAPI）
# 用于 Python 3.14 环境，使用 psycopg（有预编译包）

# FastAPI框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# 数据库 - 使用 psycopg3 (支持 Python 3.14)
psycopg==3.3.2
psycopg-binary==3.3.2
sqlalchemy==2.0.23
alembic==1.13.1
redis==5.0.1

# 数据处理
pydantic==2.5.0
pydantic-settings==2.1.0

# 网络请求
httpx==0.25.1

# 日期时间处理
python-dateutil==2.8.2
pytz==2023.3.post1

# 配置管理
python-dotenv==1.0.0
pyyaml==6.0.1

# 安全
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# 监控与日志
prometheus-client==0.19.0
